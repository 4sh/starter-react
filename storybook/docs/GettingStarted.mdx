import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Getting Started" />

# Getting started

Deux façons de consommer ce Design System : le choix se fait une fois, au
démarrage du projet.

<br/>

## Package : `pnpm add @4sh/ui-kit`

Les composants sont publiés sous forme d'**un seul package npm**, consommable
par n'importe quelle application Angular.

```bash
pnpm add @4sh/ui-kit
```

<div className="sb-callout">

**Le gestionnaire de paquets est libre.** Le package est un tarball npm standard :
`npm install @4sh/ui-kit` ou `yarn add @4sh/ui-kit` installent la même chose.
`pnpm` est celui que le Design System utilise et recommande : sa résolution
stricte fait échouer une `peerDependency` manquante au lieu de la masquer, et ses
réglages de chaîne de dépendances mettent en quarantaine les versions publiées
depuis moins de 24 h.

</div>

<div className="sb-callout">

**Pas de risque d'oubli d'installation.** `@4sh/ui-kit-react` est un **package unique**
avec des *secondary entry points* : pas une collection de packages séparés. Tous
les entry points sont présents dès l'installation, et les dépendances entre eux
se résolvent automatiquement : importer `UiDatepicker` tire tout ce dont il a
besoin (son socle de formulaire, ses icônes…) sans que vous ayez quoi que ce
soit à déclarer.

</div>

```ts
import { UiDatepicker } from '@4sh/ui-kit-react/forms/ui-datepicker';

@Component({
  selector: 'app-booking',
  imports: [UiDatepicker],
  template: `<ui-datepicker label="Date" valueType="date" [(ngModel)]="date" />`,
})
export class Booking { date = signal<Date | null>(null); }
```

Le nom de l'entry point est celui du composant : la page de doc de chaque
composant, dans la barre latérale, donne l'import exact à recopier.

### MCP (optionnel)

L'installation pose aussi le serveur MCP compagnon, sous
`node_modules/@4sh/ui-kit-react/mcp/`, mais ne le déclare nulle part : c'est la seule
étape manuelle du mode package. Pour qu'un agent IA compatible MCP (Claude Code,
Claude Desktop…) interroge la doc du kit sans lire les sources, ajoutez-le à la
racine du projet :

```json
// .mcp.json
{
  "mcpServers": {
    "ui-kit": {
      "command": "node",
      "args": ["node_modules/@4sh/ui-kit-react/mcp/index.js"]
    }
  }
}
```

<br/>
<br/>

## Schematic : `ng add @4sh/ui-kit-schematics`

L'installation livre les composants **compilés**. Le second mode de consommation
les installe en **sources**, dans votre dépôt, libres d'être modifiées (façon
shadcn/spartan-ng) :

```bash
ng add @4sh/ui-kit-schematics                    # fondation + composants + Storybook, en une commande
ng add @4sh/ui-kit-schematics --skip-storybook   # idem, sans le Storybook
ng generate @4sh/ui-kit-schematics:add           # ajouter d'autres composants ensuite
ng generate @4sh/ui-kit-schematics:update        # diff par composant après une release du kit
```

Le parcours passe par le package compagnon, et `@4sh/ui-kit-react` n'entre jamais dans
`node_modules` : absent, rien ne peut importer son code compilé à la place de vos
copies.

<div className="sb-callout">

**Pas de section MCP ici, volontairement.** `ng add` déclare déjà le serveur MCP
tout seul : `.mcp.json` (fusionné, jamais écrasé s'il existe) et `.ui-kit-mcp/` à
la racine du projet, plus une instruction ajoutée à `AGENTS.md`. Rien à faire de
plus (`--skip-mcp` pour l'omettre).

</div>

<br/>
<br/>

## Le mode d'emploi complet vit dans le README du package

Installation, liste des entry points, feuille de styles à charger, providers
attendus par `ui-image` et `BrandService`, construction de votre propre champ
sur `@4sh/ui-kit-react/forms` : tout est décrit **une seule fois**, dans le README
livré avec le package : c'est aussi ce qu'affiche la page npmjs.

<table>
  <thead>
    <tr>
      <th>Ressource</th>
      <th>Lien</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Page du package</td>
      <td><a href="https://www.npmjs.com/package/@4sh/ui-kit" target="_blank" rel="noopener noreferrer">npmjs.com/package/@4sh/ui-kit</a></td>
    </tr>
    <tr>
      <td>README (anglais)</td>
      <td><a href="https://github.com/4sh/starter-react/blob/main/packages/ui-kit-react/README.md" target="_blank" rel="noopener noreferrer">packages/ui-kit-react/README.md</a></td>
    </tr>
    <tr>
      <td>README (français)</td>
      <td><a href="https://github.com/4sh/starter-react/blob/main/packages/ui-kit-react/README.fr.md" target="_blank" rel="noopener noreferrer">packages/ui-kit-react/README.fr.md</a></td>
    </tr>
    <tr>
      <td>Dépôt</td>
      <td><a href="https://github.com/4sh/starter-react" target="_blank" rel="noopener noreferrer">github.com/4sh/starter-react</a></td>
    </tr>
  </tbody>
</table>

<div className="sb-callout">

**Pourquoi un simple renvoi ?** Ces informations étaient auparavant recopiées
ici, dans le README anglais et dans le README français. Trois copies d'une même
page d'installation divergent : c'est ainsi que le nombre d'entry points annoncé
avait fini par être faux à deux endroits sur trois. La page publique du package
fait foi ; cette page ne garde que ce qui lui est propre.

</div>

import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Typography" />

# Typographie

Système de classes utilitaires basé sur les tokens (tailles **responsives** automatiques).
Définition : `packages/ui-kit-react/src/styles/base/_typography.scss`.

## Familles de polices

Voici les polices configurées via les design tokens. Chaque famille a un rôle précis dans l'identité visuelle du projet.

<div className="sb-unstyled">
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))', gap: '20px', margin: '20px 0' }}>

    {/* Title Font */}
    <div style={{ padding: '24px', border: '1px solid var(--sb-border)', borderRadius: '12px', background: 'var(--sb-bg)' }}>
      <div style={{ fontSize: '12px', color: 'var(--sb-text-subtle)', textTransform: 'uppercase', letterSpacing: '1px', marginBottom: '8px' }}>Titres & En-têtes</div>
      <div style={{ fontSize: '32px', fontFamily: 'var(--fontfamily-title)', fontWeight: 700, lineHeight: 1, marginBottom: '12px', color: 'var(--sb-text)' }}>
        DM Sans
      </div>
      <div style={{ fontSize: '14px', color: 'var(--sb-text-subtle)', fontFamily: 'var(--fontfamily-title)' }}>
        ABCDEFGHIJKLMN<br/>
        abcdefghijklmn<br/>
        1234567890
      </div>
      <div style={{ marginTop: '16px', fontSize: '12px', background: 'var(--sb-bg-subtle)', padding: '4px 8px', borderRadius: '4px', display: 'inline-block' }}>
        <code style={{ background: 'transparent', border: 'none' }}>--fontfamily-title</code>
      </div>
    </div>

    {/* Base Font */}
    <div style={{ padding: '24px', border: '1px solid var(--sb-border)', borderRadius: '12px', background: 'var(--sb-bg)' }}>
      <div style={{ fontSize: '12px', color: 'var(--sb-text-subtle)', textTransform: 'uppercase', letterSpacing: '1px', marginBottom: '8px' }}>Corps de texte & UI</div>
      <div style={{ fontSize: '32px', fontFamily: 'var(--fontfamily-base)', fontWeight: 400, lineHeight: 1, marginBottom: '12px', color: 'var(--sb-text)' }}>
        Inter
      </div>
      <div style={{ fontSize: '14px', color: 'var(--sb-text-subtle)', fontFamily: 'var(--fontfamily-base)' }}>
        ABCDEFGHIJKLMN<br/>
        abcdefghijklmn<br/>
        1234567890
      </div>
      <div style={{ marginTop: '16px', fontSize: '12px', background: 'var(--sb-bg-subtle)', padding: '4px 8px', borderRadius: '4px', display: 'inline-block' }}>
        <code style={{ background: 'transparent', border: 'none' }}>--fontfamily-base</code>
      </div>
    </div>

    {/* Monospace Font */}
    <div style={{ padding: '24px', border: '1px solid var(--sb-border)', borderRadius: '12px', background: 'var(--sb-bg)' }}>
      <div style={{ fontSize: '12px', color: 'var(--sb-text-subtle)', textTransform: 'uppercase', letterSpacing: '1px', marginBottom: '8px' }}>Code & Technique</div>
      <div style={{ fontSize: '32px', fontFamily: 'var(--fontfamily-monospace)', fontWeight: 400, lineHeight: 1, marginBottom: '12px', color: 'var(--sb-text)' }}>
        Roboto Mono
      </div>
      <div style={{ fontSize: '14px', color: 'var(--sb-text-subtle)', fontFamily: 'var(--fontfamily-monospace)' }}>
        ABCDEFGHIJKLMN<br/>
        abcdefghijklmn<br/>
        1234567890
      </div>
      <div style={{ marginTop: '16px', fontSize: '12px', background: 'var(--sb-bg-subtle)', padding: '4px 8px', borderRadius: '4px', display: 'inline-block' }}>
        <code style={{ background: 'transparent', border: 'none' }}>--fontfamily-monospace</code>
      </div>
    </div>

  </div>
</div>

---

## Fournir les fichiers de police

Les tokens **nomment** les familles, ils ne les **livrent** pas. Ni `styles.css` (mode
package) ni la fondation copiée (mode schematics) ne contient de `@font-face`, et le kit
n'appelle aucun CDN : les fichiers de police appartiennent au projet, comme sa marque.

### Ce qui est garanti sans rien installer

Chaque `--fontfamily-*` généré se termine par une pile système. Une application qui
n'embarque aucune police s'affiche donc dans le sans-serif de l'OS : San Francisco,
Segoe UI, Roboto, et **jamais** dans le serif par défaut du navigateur :

```css
--fontfamily-base:       Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
--fontfamily-title:      "DM Sans", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
--fontfamily-monospace:  "Roboto Mono", ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
--fontfamily-icon:       "Font Awesome 6 Free";
```

La première famille de chaque pile est celle du Design System ; les suivantes sont le
filet. La pile est ajoutée **au build** par `tokens.config.json` (clé `fontStacks` de la
collection `typography`) : le JSON de tokens, lui, garde le nom de famille nu, parce que
c'est ce que la variable Figma stocke. `--fontfamily-icon` n'a volontairement pas de
repli : une police d'icônes n'a pas de substitut lisible.

### Installer les vôtres, en trois gestes

**1. Poser les fichiers** dans `src/assets/fonts/`. Un format suffit : `.woff2`, supporté
partout, et une **variable font** couvre toute la plage de graisses en une ressource.

**2. Déclarer les `@font-face`** dans une feuille **globale** du projet :

```scss
// src/styles/vendors/_fonts.scss
$fonts: "../../assets/fonts"; // chemin relatif à CE fichier : le builder Angular
                              // le réécrit et empreinte les fichiers au build

@font-face {
  font-family: "Söhne";                          // le nom que le token ira chercher
  src: url("#{$fonts}/soehne-variable.woff2") format("woff2");
  font-weight: 100 900;                          // variable font : toute la plage
  font-style: normal;
  font-display: swap;                            // texte visible pendant le chargement
}

@font-face {
  font-family: "Söhne";
  src: url("#{$fonts}/soehne-variable-italic.woff2") format("woff2");
  font-weight: 100 900;
  font-style: italic;
  font-display: swap;
}
```

**3. Pointer les tokens dessus**, dans la même feuille globale : en **gardant la queue de
pile**, sinon le serif revient au premier écran où la police n'a pas fini de charger :

```scss
:root {
  --fontfamily-base: "Söhne", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  --fontfamily-title: "Söhne Breit", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
}
```

Rien d'autre à toucher : les composants lisent tous `var(--fontfamily-base)` ou
`var(--fontfamily-title)`, jamais un nom de police en dur.

### Où déclarer, selon le mode

<table className="doc-table">
  <thead>
    <tr>
      <th>Mode</th>
      <th>Fichier</th>
      <th>Point d'attention</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Package (<code>@4sh/ui-kit</code>)</td>
      <td>Votre feuille globale, ex. <code>src/styles/main.scss</code></td>
      <td>Elle doit être chargée <strong>après</strong> <code>@4sh/ui-kit-react/styles.css</code> dans <code>angular.json</code> : les deux écrivent sur <code>:root</code>, c'est l'ordre des feuilles qui tranche.</td>
    </tr>
    <tr>
      <td>Schematics (<code>ng add</code>)</td>
      <td><code>src/styles/vendors/_fonts.scss</code>, chargé par <code>@use</code> depuis <code>src/styles/main.scss</code></td>
      <td>Le pipeline de tokens est aussi dans le dépôt : la famille peut se changer à la source, dans <code>design-tokens/typography.json</code> + <code>pnpm tokens:build</code>, plutôt que par surcharge.</td>
    </tr>
  </tbody>
</table>

L'implémentation de référence est celle de ce starter :
`src/styles/src/vendors/_fonts.scss` (DM Sans + Inter en variable fonts auto-hébergées),
chargée depuis `src/styles/main.scss`.

### Pièges

<div className="sb-callout">

- **La surcharge doit être globale.** `:root` écrit dans le `.scss` d'un composant Angular
  ne s'applique pas : l'encapsulation tamponne l'attribut de scope sur le sélecteur, et
  `:root` ne le porte jamais. Feuille globale uniquement.
- **Auto-héberger, pas de CDN.** Servir Google Fonts depuis ses serveurs transmet l'IP du
  visiteur à un tiers, c'est le point que la CNIL sanctionne, et casse tout build ou
  déploiement hors ligne.
- **Ne jamais laisser une famille seule.** `font-family: "Söhne"` sans queue de pile, c'est
  exactement le serif du navigateur qui revient dès que le fichier manque.
- **`font-display: swap`** évite l'écran de texte invisible pendant le téléchargement.
- **La police d'icônes est à part.** `--fontfamily-icon` ne fait que la nommer ; la feuille
  FontAwesome se charge séparément (`node_modules/@fortawesome/fontawesome-free/css/all.css`
  dans `angular.json`, `ng add` la câble automatiquement).

</div>

---

<table className="doc-table">
  <thead>
    <tr>
      <th>Famille</th>
      <th>Classes</th>
      <th>Tailles</th>
    </tr>
  </thead>
  <tbody>
    <tr><td>Titres</td><td><code>.title-&#123;size&#125;</code></td><td>sm, md, default, lg, xl, 2xl</td></tr>
    <tr><td>Textes</td><td><code>.text-&#123;weight&#125;-&#123;size&#125;</code></td><td>weight: light, regular, bold, italic : size: sm, md, default, lg, xl</td></tr>
    <tr><td>Accent</td><td><code>.accent-&#123;size&#125;</code></td><td>sm, default, lg</td></tr>
  </tbody>
</table>

export const Row = ({ cls, sample }) => (
  <div style={{ display: 'flex', alignItems: 'baseline', gap: 20, padding: '6px 0', borderBottom: '1px solid var(--sb-border,#eee)' }}>
    <code style={{ fontSize: 12, minWidth: 180, flexShrink: 0 }}>{cls}</code>
    <span className={`sb-unstyled ${cls.slice(1)}`}>{sample || 'The quick brown fox : 0123'}</span>
  </div>
);

## Titres

export const TITLES = ['sm','md','default','lg','xl','2xl'];

{TITLES.map((s) => <Row key={s} cls={`.title-${s}`} sample="Titre de section" />)}

## Textes

export const TWEIGHTS = ['light','regular','bold','italic'];
export const TSIZES = ['sm','md','default','lg','xl'];

{TWEIGHTS.map((w) => (
  <div key={w}>
    <h4 style={{ marginTop: 24, textTransform: 'capitalize' }}>{w}</h4>
    {TSIZES.map((s) => <Row key={s} cls={`.text-${w}-${s}`} />)}
  </div>
))}

## Accent

export const ASIZES = ['sm','default','lg'];

{ASIZES.map((s) => <Row key={s} cls={`.accent-${s}`} sample="Texte d'accentuation" />)}

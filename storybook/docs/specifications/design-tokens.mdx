import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Spécifications/Design Tokens" />

# Design Tokens & Theming System

Cette page récapitule le fonctionnement et l'usage des Design Tokens du projet.<br/>
Notre architecture repose sur une source de vérité centralisée (Figma) et une génération automatisée.

## 1. Cycle de vie des Tokens (Workflow)

Il est essentiel de comprendre le flux de nos variables pour maintenir la cohérence du Design System :

* **Source de vérité (Figma & JSON)** : Les variables sont extraites de fichiers JSON situés dans `design-tokens/`.
* **Génération automatisée** : Un script `scripts/tokens.build.mjs` compile ces données en plusieurs formats cibles.
* **Fichiers SCSS/CSS (`.scss`)** : Générés dans `packages/ui-kit-react/src/styles/generated/`, ce sont les variables (ex: `var(--...)`) que vous devez utiliser lorsque vous écrivez du style pour vos éléments de layout / composants.

<div className="ai-instruction-block">
    <div className="ai-instruction-block-title">
        INSTRUCTIONS POUR L'IA ET LES DÉVELOPPEURS
    </div>
    <ul>
        <li><strong>Aucune création manuelle</strong> : Si un token exact manque dans le CSS, ne l'inventez pas en dur. Il doit d'abord être créé côté Figma/JSON. En attendant, déduisez et utilisez le fallback sémantique le plus proche.</li>
        <li><strong>Ne jamais utiliser de valeurs en dur</strong> (ex: <code>#ffffff</code> ou <code>16px</code>).</li>
        <li><strong>Préférez les variables CSS natives</strong> (<code>var(--...)</code>) aux variables SASS (<code>$my-var</code>) pour les couleurs afin de supporter le thème dynamique (Dark Mode).</li>
        <li>Pour les espacements, dimensions, radius utilisez exclusivement les variables <code>--units-* / --spacing-* / --radius-*</code>.</li>
    </ul>
</div>


## 2. Familles de Tokens

Les tokens sont divisés en plusieurs fichiers dans `packages/ui-kit-react/src/styles/generated/` :

- **`_tokens-semantics.scss`** : Couleurs liées à une fonction d'interface (ex: action, feedback, navigation).
- **`_tokens-metrics.scss`** : Unités de mesure structurelles (espacement, arrondi, bordure).
- **`_tokens-typography.scss`** : Styles de texte (familles, graisses, tailles).
- **`_tokens-breakpoints.scss`** : Définition des points de rupture responsive.

## 3. Tokens Sémantiques (Couleurs)

Utilisez ces tokens pour garantir la cohérence visuelle et le support automatique du **Dark Mode**.

### Structure des Noms (Anatomie)

La nomenclature suit une logique de construction stricte : `[famille]-[niveau]-[propriété]-[état]`.
Les tokens sémantiques sont générés **sans préfixe** : la famille ouvre directement le nom
(`--actions-high-surface-default`), il n'y a pas de segment `token-` ou `semantics-`.

*(Le domaine `global` suit une anatomie volontairement plus courte, en trois segments :
voir [Anatomie du domaine `global`](#anatomie-du-domaine-global) ci-dessous.)*

<table className="doc-table">
    <thead>
    <tr>
        <th>Segment</th>
        <th>Valeurs possibles</th>
        <th>Rôle</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td><strong>1. Famille</strong></td>
        <td><code>global</code>, <code>actions</code>, <code>form</code>, <code>informative</code>, <code>navigation</code>, <code>table</code></td>
        <td>L'intention fonctionnelle. <code>global</code> = structurel (fondations), les autres = liées à un type de composant.</td>
    </tr>
    <tr>
        <td><strong>2. Niveau</strong></td>
        <td><code>high</code>, <code>low</code>, <code>success</code>, <code>error</code>, <code>warning</code>...</td>
        <td>La hiérarchie visuelle ou la variante sémantique.</td>
    </tr>
    <tr>
        <td><strong>3. Propriété</strong></td>
        <td><code>surface</code>, <code>content</code>, <code>stroke</code></td>
        <td>La cible CSS : <em>surface</em> (fond), <em>content</em> (texte/icône), <em>stroke</em> (bordure).</td>
    </tr>
    <tr>
        <td><strong>4. État</strong></td>
        <td><code>default</code>, <code>hover</code>, <code>focused</code>, <code>pressed</code>, <code>disabled</code>, <code>active</code>, <code>checked</code>, <code>odd</code>, <code>even</code></td>
        <td>L'état d'interaction de l'élément (pseudo-classes CSS).</td>
    </tr>
    </tbody>
</table>

> **Casse** : le JSON est en camelCase, le générateur met en minuscules **sans séparateur**.
> `informative.defaultHigh` donne donc `--informative-defaulthigh-*`, et non
> `--informative-default-high-*`. En cas de doute, la source est
> `packages/ui-kit-react/src/styles/generated/_tokens-semantics.scss`.

> **Hors anatomie** : la famille `effects` est **plate** (`--effects-default`,
> `--effects-highlight`, `--effects-success`, `--effects-warning`, `--effects-error`) : pas de
> niveau, ni de propriété, ni d'état. Ce sont les couleurs de diffusion qui alimentent les
> ombres composites (voir §5).

### Le segment `[niveau]` est souvent composé

C'est la principale source de tâtonnement quand on cherche un token : `[niveau]` n'est pas
toujours un simple `high`/`low`. Il **fusionne** régulièrement le niveau avec un second axe, et
la casse disparaissant à la génération (voir ci-dessus), le nom final peut surprendre.

<table className="doc-table">
    <thead>
    <tr>
        <th>Second axe fusionné</th>
        <th>Familles</th>
        <th>Exemple</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td><strong>Emphase</strong> (<code>High</code> / <code>Low</code>)</td>
        <td><code>informative</code></td>
        <td><code>--informative-errorhigh-surface-default</code><br/>(<code>informative.errorHigh</code>)</td>
    </tr>
    <tr>
        <td><strong>Variante d'apparence</strong> (<code>Outlined</code> / <code>Ghost</code>)</td>
        <td><code>actions</code></td>
        <td><code>--actions-successoutlined-content-hover</code><br/>(<code>actions.successOutlined</code>)</td>
    </tr>
    <tr>
        <td><strong>Polarité de fond</strong> : le niveau est <em>remplacé</em> par <code>on[Polarité]</code>, suivi de la variante, puis du niveau seulement là où il reste porteur de sens</td>
        <td><code>actions</code></td>
        <td><code>--actions-ondark-outlined-stroke-hover</code><br/><code>--actions-ondark-filled-error-content-default</code></td>
    </tr>
    </tbody>
</table>

<div className="ai-instruction-block">
    <div className="ai-instruction-block-title">
        EXCEPTION À CONNAÎTRE : LES JEUX <code>onDark</code> / <code>onLight</code>
    </div>
    <p>
        C'est la <strong>seule famille du DS dont les valeurs <code>modeLight</code> et
        <code>modeDark</code> sont volontairement identiques</strong>. Elle sert à un composant
        posé sur un fond de couleur (bandeau, carte teintée, image) : ce fond ne change pas avec
        le thème, donc la polarité du composant ne doit pas basculer non plus. Ce n'est pas un
        oubli de mode sombre, ne « corrigez » pas ces tokens pour les faire diverger.
    </p>
    <p>
        N'utilisez ces jeux <strong>que</strong> par-dessus une surface de couleur
        maîtrisée. Sur le fond de page, ce sont les jeux normaux qui s'appliquent, eux bien
        réactifs au thème. Choix de la polarité et domaine de validité :
        <a href="?path=/docs/components-ui-actions-ui-button--docs"><code>ui-button</code> → onColor</a>.
    </p>
</div>

### Anatomie du domaine `global`

Le domaine `global` porte les couleurs **structurelles** de l'application (fonds de page,
textes courants, séparateurs). Il n'a ni famille ni niveau : sa nomenclature est
`global-[axe]-[occurrence]`, en trois segments.

Ce choix est délibéré. Les mots `high`/`low` des familles fonctionnelles désignent une
**hiérarchie d'intention** (bouton primaire vs secondaire) ; appliqués à des neutres ils
désignaient un **niveau de contraste**. Deux sens pour un même mot : l'axe a donc été
remplacé par un vocabulaire d'emphase explicite, et l'axe `[propriété]` par les trois
propriétés CSS qu'il cible réellement.

<table className="doc-table">
    <thead>
    <tr>
        <th>Segment</th>
        <th>Valeurs possibles</th>
        <th>Rôle</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td><strong>1. Axe</strong></td>
        <td><code>background</code>, <code>text</code>, <code>border</code></td>
        <td>La cible CSS, nommée d'après la propriété : <code>background-color</code>, <code>color</code>, <code>border-color</code>.</td>
    </tr>
    <tr>
        <td><strong>2. Occurrence</strong></td>
        <td>
            <em>emphase</em> : <code>default</code>, <code>muted</code>, <code>subtle</code><br/>
            <em>rôle</em> : <code>brand</code>, <code>focus</code>, <code>inverse</code>, <code>disabled</code>
        </td>
        <td>Soit le degré d'emphase du neutre, soit un rôle particulier. Deux familles de mots, au même niveau.</td>
    </tr>
    <tr>
        <td><strong>3. État</strong> <em>(optionnel)</em></td>
        <td><code>hover</code></td>
        <td>Suffixé à l'occurrence (<code>default-hover</code>) et <strong>omis à l'état de repos</strong> : d'où les trois segments du cas courant.</td>
    </tr>
    </tbody>
</table>

```scss
.ui-card {
  background-color: var(--global-background-default);  // fond de la surface
  color: var(--global-text-default);                   // texte principal
  border-color: var(--global-border-subtle);           // bordure discrète

  .ui-card-subtitle { color: var(--global-text-muted); }        // texte atténué
  &:focus-visible { box-shadow: 0 0 0 2px var(--global-border-focus); }
}
```

> L'inventaire exhaustif et les valeurs résolues (par thème, marque et mode) sont dans
> [Foundations / Colors](?path=/docs/foundations-colors--docs), généré depuis
> `tokens.manifest.json`.

### Référentiel des Familles Fonctionnelles

Utilisez ce tableau pour déterminer quelle `[famille]` utiliser en se basant strictement sur **l'intention fonctionnelle** du composant (les listes de composants ne sont données qu'à titre d'exemple non exhaustif).

<table className="doc-table">
    <thead>
    <tr>
        <th>Famille</th>
        <th>Usage Intentionnel (Heuristique)</th>
        <th>Exemple de construction</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td><strong><code>global</code></strong></td>
        <td>
            <strong>Règle :</strong> Couleurs de fondation de l'application.<br/>
            <em>Exemples : Fonds d'écran, surfaces générales, textes par défaut, séparateurs structurels.</em>
        </td>
        <td><code>--global-high-surface-default</code></td>
    </tr>
    <tr>
        <td><strong><code>actions</code></strong></td>
        <td>
            <strong>Règle :</strong> TOUT élément interactif qui déclenche un événement, une soumission ou une action.<br/>
            <em>Exemples non exhaustifs : Boutons, liens, déclencheurs d'accordéon, pagination, etc.</em>
        </td>
        <td><code>--actions-high-surface-hover</code></td>
    </tr>
    <tr>
        <td><strong><code>form</code></strong></td>
        <td>
            <strong>Règle :</strong> TOUT élément permettant à l'utilisateur de saisir, modifier ou sélectionner une donnée.<br/>
            <em>Exemples non exhaustifs : Inputs texte, checkbox, radio, select, sliders, toggles, color pickers, etc.</em>
        </td>
        <td><code>--form-high-stroke-focused</code></td>
    </tr>
    <tr>
        <td><strong><code>informative</code></strong></td>
        <td>
            <strong>Règle :</strong> Retours visuels non interactifs affichant un statut, un feedback ou mettant en valeur une donnée.<br/>
            <em>Exemples non exhaustifs : Alertes, badges, tags, toasts, tooltips, barres de progression, etc.</em>
        </td>
        <td><code>--informative-errorhigh-surface-default</code></td>
    </tr>
    <tr>
        <td><strong><code>navigation</code></strong></td>
        <td>
            <strong>Règle :</strong> Éléments d'architecture servant exclusivement à guider l'utilisateur dans les vues de l'application.<br/>
            <em>Exemples non exhaustifs : Menus, onglets, sidebars, fil d'Ariane, steppers, etc.</em>
        </td>
        <td><code>--navigation-high-content-active</code></td>
    </tr>
    <tr>
        <td><strong><code>table</code></strong></td>
        <td>
            <strong>Règle :</strong> Strictement réservé à l'affichage de données tabulaires complexes.<br/>
            <em>Exemples : DataTables, grilles, en-têtes, lignes paires/impaires, cellules.</em>
        </td>
        <td><code>--table-body-surface-odd</code></td>
    </tr>
    </tbody>
</table>

## 4. Tokens Métriques (Metrics)

<table className="doc-table">
    <thead>
    <tr>
        <th>Préfixe</th>
        <th>Usage</th>
        <th>Exemple</th>
        <th style={{ textAlign: 'center' }}>Valeur Défaut</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td><strong><code>--units-</code></strong></td>
        <td>Espacements (margin, padding, gap)</td>
        <td><code>--units-md</code></td>
        <td style={{ textAlign: 'center' }}><code>12px</code></td>
    </tr>
    <tr>
        <td><strong><code>--radius-</code></strong></td>
        <td>Arrondis (border-radius)</td>
        <td><code>--radius-default</code></td>
        <td style={{ textAlign: 'center' }}><code>12px</code></td>
    </tr>
    <tr>
        <td><strong><code>--stroke-</code></strong></td>
        <td>Épaisseur de bordure (border-width)</td>
        <td><code>--stroke-default</code></td>
        <td style={{ textAlign: 'center' }}><code>1px</code></td>
    </tr>
    </tbody>
</table>

## 5. Styles composites (`styles.json`)

Certains tokens Figma sont **composites** (plusieurs propriétés en un style : ombres…). Ils
vivent dans `design-tokens/styles.json` et sont générés différemment : le pipeline
**compose** une valeur CSS prête à l'emploi et l'expose en **double** : variable + classe
utilitaire : pour un usage aussi bien en SCSS qu'en HTML.

- Sortie : `--shadow-{catégorie}-{taille}` (valeur `box-shadow`) + `.shadow-{catégorie}-{taille}`.
- Config : output `format: "composite-styles"` dans `tokens.config.json`. Ajouter un type
  composite = étendre la table `COMPOSERS` dans `scripts/tokens.build.mjs`.
- Détail et aperçu : **Foundations → Shadows**.

## 6. Exemples d'Implémentation

```scss
// ✅ BON USAGE
.mon-composant {
  background-color: var(--global-high-surface-default);
  padding: var(--units-md);
  border-radius: var(--radius-default);
}

// ❌ À ÉVITER
.mon-composant {
  background-color: white; // Ne gère pas le Dark Mode
  padding: 12px;           // Valeur en dur interdite
}
```

## 7. Constantes structurelles partagées (`ui-config`)

En complément des tokens (thémables, runtime), les composants `ui-*` partagent des
**constantes structurelles** SASS (focus ring, bordure, taille des contrôles, transition) :
le *choix* du token pour un rôle donné, mutualisé et surchargeable à 3 niveaux
(global UI → catégorie → composant).

> Référentiel complet des variables, valeurs par défaut et mécanismes de surcharge :
> **Spécifications → Configuration (ui-config)**.

import { Meta } from '@storybook/addon-docs/blocks';
import { SharedConfigTable } from '../../blocks/config-table';

<Meta title="Components/configuration/Forms" />

# Configuration : catégorie Forms

Constantes de la famille **forms** (`$form-*`). Chaque variable hérite par défaut d'une valeur
globale et peut être surchargée pour **tous** les contrôles de formulaire à la fois.

> Fichier source : `packages/ui-kit-react/src/styles/settings/_ui-config.scss` · exposé via `@use 'utils'`.
> Consommée par : `ui-checkbox`, `ui-radio`, `ui-toggle`, `ui-field` (donc `ui-input`,
> `ui-input-mask`, `ui-input-number`, `ui-textarea`). Retour au
> [fonctionnement & sommaire](?path=/docs/components-configuration--docs).

## Contrôles (`$form-control-*`)

<SharedConfigTable group="forms" prefix="form-control-" />

## Boîte de champ (`$form-field-*`)

<SharedConfigTable group="forms" prefix="form-field-" />

## Multiligne (`$form-textarea-*`)

<SharedConfigTable group="forms" prefix="form-textarea-" />

## Focus

<SharedConfigTable group="forms" prefix="form-focus-" />

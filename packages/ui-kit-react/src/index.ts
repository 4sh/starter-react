/*
 * Entrée volontairement minimale : chaque composant a son sous-chemin
 * (`@4sh/ui-kit-react/ui-icon`), et le réexporter ici annulerait le découpage.
 */

/** Version du paquet, alignée sur `package.json` au moment de la publication. */
export const UI_KIT_VERSION = '0.1.0';
